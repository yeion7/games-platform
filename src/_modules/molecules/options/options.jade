mixin options(spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''

  div(class=spec.__class)&attributes(attributes)
    i(class="icon-reload game-options-reload")
    i(class="icon-check  game-options-check")
    
